from django.urls import path
#from .views import  repair_vehicle, relocate_vehicle
from . import views


urlpatterns = [
    #path('', views.operator_main, name='operator_main'),
    #path('repair/<int:vehicle_id>/', repair_vehicle, name='repair_vehicle'),
    #path('relocate/<int:vehicle_id>/', relocate_vehicle, name='relocate_vehicle'),
]
